# `clear` Workflow Command

The `clear` command is used to clear all saved workflows. This command does not require any arguments or options.

## Usage

```sh
letrun workflow clear
```

### Examples

#### Clear all saved workflows

```sh
letrun workflow clear
```
